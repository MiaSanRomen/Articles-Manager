class Article{
    constructor(id, name, intro, about, image, blocks) {
        this.id = id
        this.name = name
        this.intro = intro
        this.about = about
        this.image = image
        this.blocks = blocks
    }
}